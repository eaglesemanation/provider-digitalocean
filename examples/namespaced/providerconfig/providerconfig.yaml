apiVersion: digitalocean.m.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: digitalocean
  namespace: crossplane-system
spec:
  credentials:
    source: Secret
    secretRef:
      name: example-creds
      namespace: crossplane-system
      key: credentials
